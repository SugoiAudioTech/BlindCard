# BlindCard 撲克牌主題色彩系統

> 基於深炭黑牌桌風格 (02_felt_dark)

---

## 基礎色票

### 背景層級 (Background)

| 名稱 | Hex | RGB | 用途 |
|------|-----|-----|------|
| `bg-base` | `#0A0A0A` | 10, 10, 10 | 最底層背景 |
| `bg-surface` | `#141414` | 20, 20, 20 | 卡片、面板背景 |
| `bg-elevated` | `#1E1E1E` | 30, 30, 30 | 浮動元件、下拉選單 |
| `bg-hover` | `#252525` | 37, 37, 37 | Hover 狀態背景 |

### 邊框與分隔 (Border)

| 名稱 | Hex | RGB | 用途 |
|------|-----|-----|------|
| `border-subtle` | `#1F1F1F` | 31, 31, 31 | 微弱分隔線 |
| `border-default` | `#2A2A2A` | 42, 42, 42 | 一般邊框 |
| `border-strong` | `#3A3A3A` | 58, 58, 58 | 明顯邊框 |

### 文字層級 (Text)

| 名稱 | Hex | RGB | 用途 |
|------|-----|-----|------|
| `text-primary` | `#F5F5F0` | 245, 245, 240 | 主要文字（米白） |
| `text-secondary` | `#A0A0A0` | 160, 160, 160 | 次要文字 |
| `text-muted` | `#606060` | 96, 96, 96 | 禁用/提示文字 |
| `text-inverse` | `#0A0A0A` | 10, 10, 10 | 反白文字（用於亮色按鈕上） |

---

## 品牌強調色 (Brand Accent)

### 紅色系 - 賭桌邊緣光 (Casino Red)

| 名稱 | Hex | RGB | 用途 |
|------|-----|-----|------|
| `red-glow` | `#FF3B4E` | 255, 59, 78 | 霓虹發光效果 |
| `red-primary` | `#E63946` | 230, 57, 70 | 主要紅色（按鈕、警告） |
| `red-dark` | `#B82D3A` | 184, 45, 58 | Hover/Pressed 狀態 |
| `red-subtle` | `#3D1A1E` | 61, 26, 30 | 紅色背景提示 |

### 金色系 - Reveal 揭曉光 (Reveal Gold)

| 名稱 | Hex | RGB | 用途 |
|------|-----|-----|------|
| `gold-glow` | `#FFD700` | 255, 215, 0 | 發光效果 |
| `gold-primary` | `#D4AF37` | 212, 175, 55 | 主要金色 |
| `gold-dark` | `#B8962E` | 184, 150, 46 | Hover/Pressed 狀態 |
| `gold-subtle` | `#2D2815` | 45, 40, 21 | 金色背景提示 |

### 籌碼配色 (Chip Colors)

| 名稱 | Hex | RGB | 用途 |
|------|-----|-----|------|
| `chip-red` | `#8B2D3A` | 139, 45, 58 | 籌碼紅（深沉） |
| `chip-white` | `#F5F5E8` | 245, 245, 232 | 籌碼白（象牙色） |
| `chip-gold` | `#C9A84C` | 201, 168, 76 | 籌碼金邊 |

---

## 功能色 (Semantic Colors)

### 狀態指示

| 名稱 | Hex | 用途 |
|------|-----|------|
| `status-active` | `#D4AF37` | 啟用狀態（金色） |
| `status-warning` | `#E63946` | 警告狀態（紅色） |
| `status-success` | `#4CAF50` | 成功狀態（綠色） |
| `status-disabled` | `#404040` | 禁用狀態 |

### 音訊專用

| 名稱 | Hex | 用途 |
|------|-----|------|
| `audio-input` | `#4A90D9` | 輸入訊號（藍） |
| `audio-output` | `#50C878` | 輸出訊號（綠） |
| `audio-clip` | `#FF3B4E` | 爆音警告（紅） |

---

## 漸層定義 (Gradients)

### 牌桌絨布漸層
```css
background: radial-gradient(
  ellipse at center,
  #1A1A1A 0%,
  #0F0F0F 50%,
  #0A0A0A 100%
);
```

### 紅色霓虹邊框
```css
border-color: #E63946;
box-shadow:
  0 0 10px rgba(230, 57, 70, 0.5),
  0 0 20px rgba(230, 57, 70, 0.3),
  inset 0 0 10px rgba(230, 57, 70, 0.1);
```

### Reveal 金光效果
```css
box-shadow:
  0 0 20px rgba(212, 175, 55, 0.6),
  0 0 40px rgba(212, 175, 55, 0.4),
  0 0 60px rgba(212, 175, 55, 0.2);
```

---

## 元件對應表

| UI 元件 | 背景 | 邊框 | 文字 | 強調 |
|---------|------|------|------|------|
| 主背景 | `bg-base` | - | - | - |
| Control Bar | `bg-surface` | `border-subtle` | `text-primary` | - |
| 一般按鈕 | `bg-elevated` | `border-default` | `text-primary` | - |
| 一般按鈕 (Hover) | `bg-hover` | `border-strong` | `text-primary` | - |
| 啟用按鈕 | `gold-subtle` | `gold-primary` | `gold-primary` | `gold-glow` |
| 警告按鈕 | `red-subtle` | `red-primary` | `red-primary` | `red-glow` |
| Reveal 按鈕 | `bg-elevated` | `gold-primary` | `gold-primary` | `gold-glow` |
| Bypass 狀態 | `red-subtle` | `red-primary` | `text-primary` | `red-glow` |
| Muted 警告 | `red-subtle` | `red-primary` | `chip-white` | - |

---

## JUCE 色彩變數對應

```cpp
// ControlBarColors.h 建議修改

// Background
static const Colour bgBase        { 0xFF0A0A0A };
static const Colour bgSurface     { 0xFF141414 };
static const Colour bgElevated    { 0xFF1E1E1E };
static const Colour bgHover       { 0xFF252525 };

// Border
static const Colour borderSubtle  { 0xFF1F1F1F };
static const Colour borderDefault { 0xFF2A2A2A };

// Text
static const Colour textPrimary   { 0xFFF5F5F0 };
static const Colour textSecondary { 0xFFA0A0A0 };
static const Colour textMuted     { 0xFF606060 };

// Accent - Red
static const Colour redGlow       { 0xFFFF3B4E };
static const Colour redPrimary    { 0xFFE63946 };
static const Colour redDark       { 0xFFB82D3A };
static const Colour redSubtle     { 0xFF3D1A1E };

// Accent - Gold
static const Colour goldGlow      { 0xFFFFD700 };
static const Colour goldPrimary   { 0xFFD4AF37 };
static const Colour goldDark      { 0xFFB8962E };
static const Colour goldSubtle    { 0xFF2D2815 };

// Chip Colors
static const Colour chipRed       { 0xFF8B2D3A };
static const Colour chipWhite     { 0xFFF5F5E8 };
static const Colour chipGold      { 0xFFC9A84C };
```

---

*Generated by Sugoi Audio Visual Director - 2026-01-17*
